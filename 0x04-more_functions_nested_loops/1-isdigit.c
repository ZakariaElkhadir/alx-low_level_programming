#include "main.h"
int _isdigit(int c){
  /* This loop will print the characters from 0 to 9. */
  for (c = '0'; c <= '9'; c++) {
    /* This function prints a character. */
    printf("%c\n", c);
  }
}
