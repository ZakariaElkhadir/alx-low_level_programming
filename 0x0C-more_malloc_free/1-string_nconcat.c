#include "main.h"
#include <stdlib.h>
char *string_nconcat(char *s1, char *s2, unsigned int n){
	char *ptr = s1;
	char s2;
	int n;
	s1 = malloc(s2);
	s2 = string_nconcat("hello world ", 6);
	if (ptr == NULL){
		return NULL;
		free (ptr);
	}
	if (n => s2){
		printf("%s", s2);
	}
	else {
		return NULL;
	}
}
